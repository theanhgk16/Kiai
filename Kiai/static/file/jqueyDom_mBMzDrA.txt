* Thao Tác HTML DOM
- text(): innerText
- html(): inner HTML
- val() : thao tác lấy giá trị trong form
- attr() : lấy gia strị 1 thuộc tính hoặc thay đổi 1 thuộc tính
- phương thức :
+ append(): thêm vào cuối (thêm bên trong thành phần) của các thành phần ví dụ: append(li)
+ prepend(): thêm vào đầu (thêm bên trong thành phần)
+ after(): 

- dùng thay đổi gia trị CSS:
+ addClass():
+ removeClass():
+ toggleClas():
+ CSS():